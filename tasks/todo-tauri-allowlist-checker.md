## Triage: Tauri allowlist checker & CI enforcement

### Overview

This checklist captures the reviewer's discovery and an actionable planner specification. It includes the exact implementation artifacts for the `@engineer` agent (checker script and CI workflow) — do NOT implement these in the planner. The file also calls out which parts require changes in `src-tauri` (Tauri/Rust) vs frontend-only work.

### Discovery (summary)

- Frontend currently uses IPC via `invoke('...')` to call Tauri commands: `get_transactions`, `get_accounts`, `get_categories` (see `src/services/tauri-api.ts`).
- Rust side (`src-tauri/src/commands.rs`) exposes `#[tauri::command]` functions with matching names plus `get_app_path`.
- No other Tauri APIs (`fs`, `dialog`, `notification`, etc.) were detected in the frontend scan.

----

### Planner tasks (compact)

- T1 — Add checker & CI
  - Add `scripts/tauri-allowlist-checker.js` to repo root.
  - Add `.github/workflows/ci.yml` with the described job that runs frontend tests, Rust tests, and the checker.
  - Acceptance: PR triggers CI; `allowlist-report.json` produced at repo root; job fails if `tauri.allowlist.all` === `true`.

- T2 — Replace `allowlist.all: true`
  - Use the checker report to create a minimal `src-tauri/tauri.conf.json` allowlist that lists only needed APIs.
  - Acceptance: CI passes; `allowlist-report.json` indicates only expected frontend uses.

- T3 — Enforce PR policy
  - Update PR template to require a checkbox: "tauri allowlist updated & justified" for PRs that add Tauri APIs.

- T4 — Audit cadence
  - Add a recurring team task (sprint checklist item) to review `tauri.conf.json` and `allowlist-report.json` each sprint.

----

### Which parts require Tauri command (Rust) vs frontend logic

- Tauri/Rust work (src-tauri):
  - Update `tauri.conf.json` allowlist keys (`src-tauri/tauri.conf.json`). This is configuration and must be validated against actual `#[tauri::command]` and any native API usage.
  - Ensure `src-tauri/src/commands.rs` only exposes intended `#[tauri::command]` functions; if new commands are added, add them to the minimal allowlist.

- Frontend-only work (src/):
  - The checker scans frontend sources (`src/`) for Tauri API usage patterns and is a pure static check.
  - If frontend calls new Tauri APIs (e.g., `@tauri-apps/api/fs`), the frontend change triggers the PR policy and requires a corresponding update to `tauri.conf.json`.

----

### What to set aside vs what to pass to the engineer

- Set aside (planner / reviewer artifacts):
  - The reviewer's descriptive notes and rationale (keep as reference).
  - The allowlist-report.json output — this is generated by the checker at runtime and does not need to be hand-edited.

- Pass to `@engineer` (explicit actionable items):
  - Add `scripts/tauri-allowlist-checker.js` (full script below).
  - Add CI workflow `.github/workflows/ci.yml` (full YAML below).
  - Add PR template checkbox and any CI gating for merges.
  - Task T2 requires manual mapping: engineer must map found frontend API usages to the correct `tauri.conf.json` allowlist keys (human judgment required). Document mapping decisions in the PR.

----

### Implementation task (copy this to @engineer — do NOT implement from Planner)

Below is the implementation payload the reviewer suggested. Copy/paste this entire section for the `@engineer` agent.

1) scripts/tauri-allowlist-checker.js

// Node 18+ script — finds frontend @tauri uses and Rust #[tauri::command] functions,
// writes allowlist-report.json and exits non-zero if tauri.conf.json.allowlist.all === true.

const fs = require('fs');
const path = require('path');

function readFiles(dir, exts){
  const out = [];
  const stack = [dir];
  while(stack.length){
    const p = stack.pop();
    if(!fs.existsSync(p)) continue;
    for(const name of fs.readdirSync(p)){
      const fp = path.join(p,name);
      const st = fs.statSync(fp);
      if(st.isDirectory()) stack.push(fp);
      else {
        if(exts.includes(path.extname(name))) out.push(fp);
      }
    }
  }
  return out;
}

function scanPatterns(files, patterns){
  const used = new Set();
  for(const f of files){
    const s = fs.readFileSync(f,'utf8');
    for(const [key, rx] of Object.entries(patterns)){
      if(rx.test(s)) used.add(key);
    }
  }
  return [...used];
}

const repoRoot = path.resolve(__dirname,'..');
const frontFiles = readFiles(path.join(repoRoot,'src'),['.ts','.tsx','.js','.jsx']);
const rustFiles = readFiles(path.join(repoRoot,'src-tauri','src'),['.rs']);

const frontPatterns = {
  'invoke': /invoke\s*\(/,
  'window.tauri': /window\.tauri/,
  '@tauri-api': /@tauri-apps\/api/,
  'fs-api': /@tauri-apps\/api\/fs|fs\.(read|write|exists)/,
  'dialog-api': /@tauri-apps\/api\/dialog|dialog\./,
  'http-api': /@tauri-apps\/api\/http|http\./
};

const rustPatterns = {
  'tauri_command': /#\s*\[\s*tauri::command\s*]/,
  'tauri::api': /tauri::api::/
};

const frontendUses = scanPatterns(frontFiles, frontPatterns);
const rustUses = scanPatterns(rustFiles, rustPatterns);

// extract command fn names from Rust files
const commandNames = [];
for(const f of rustFiles){
  const s = fs.readFileSync(f,'utf8');
  if(/#\s*\[\s*tauri::command\s*]/.test(s)){
    const re = /#\s*\[\s*tauri::command\s*]\s*[\r\n\s]*pub\s+fn\s+([a-zA-Z0-9_]+)/g;
    let m;
    while((m=re.exec(s))){ commandNames.push(m[1]); }
  }
}

// read tauri.conf.json allowlist
let tauriConf = {};
const confPath = path.join(repoRoot,'src-tauri','tauri.conf.json');
if(fs.existsSync(confPath)){
  try { tauriConf = JSON.parse(fs.readFileSync(confPath,'utf8')); } catch(e) { /* ignore */ }
}

const allowAll = tauriConf?.tauri?.allowlist?.all === true;

const report = {
  timestamp: new Date().toISOString(),
  frontendUses,
  rustUses,
  commandNames,
  tauriAllowlist: tauriConf?.tauri?.allowlist || null,
  allowAll
};

fs.writeFileSync(path.join(repoRoot,'allowlist-report.json'), JSON.stringify(report,null,2), 'utf8');

console.log('allowlist-report.json written. Summary:');
console.log(JSON.stringify({frontendUses, rustUses, commandNames, allowAll}, null, 2));

if(allowAll){
  console.error('ERROR: tauri.conf.json allowlist.all is true — please replace with a minimal allowlist.');
  process.exit(1);
}
process.exit(0);


2) .github/workflows/ci.yml

name: CI
on: [push, pull_request]

jobs:
  test-and-check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install JS deps
        run: npm ci
      - name: Run frontend tests
        run: npx vitest run
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
      - name: Run Rust tests
        run: cd src-tauri && cargo test --verbose
      - name: Run allowlist checker
        run: node scripts/tauri-allowlist-checker.js

Notes:
- Run the job on Windows to match Tauri native build environment and MSVC expectations.
- The checker fails the job if `allowlist.all` is still `true` and writes `allowlist-report.json`.

----

### Manual mapping & engineer guidance

- The checker reports candidate frontend usages (e.g., `@tauri-apps/api/fs` or `invoke(`). The engineer must:
  1. Confirm whether a reported usage corresponds to a Tauri allowlist key (human review required).
  2. Update `src-tauri/tauri.conf.json` to include only the minimal allowlist keys required (document changes in the PR description).
  3. If new native APIs are introduced, ensure CI tests and the checker are updated if necessary.

### Acceptance criteria

- CI on PRs runs and produces `allowlist-report.json`.
- CI fails if `src-tauri/tauri.conf.json` contains `allowlist.all: true`.
- PRs that introduce Tauri APIs include an updated allowlist and a checked PR checkbox.

### How to run the checker locally (for reviewer/engineer)

From repo root:

```
node scripts/tauri-allowlist-checker.js
```

This creates `allowlist-report.json` at repo root and exits non-zero if `allowlist.all: true`.

----

### Next step

Pass this file (or the implementation section) to the `@engineer` persona in a fresh session and ask them to implement T1 and T3, then perform T2 with human-reviewed mapping.

End of planner checklist.
